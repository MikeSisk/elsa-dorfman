%nav
  .train_line_wrap
    %p.label Ginsburge
    .train_line.ginsburge
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
  .train_line_wrap
    %p.label No Hair Day
    .train_line.no-hair-day
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
  .train_line_wrap
    %p.label Portraits
    .train_line.portraits
      - @portraits_stops.each do |nl|
        = link_to "", nl.dest, :class => "stop", :label => nl.label
  .train_line_wrap
    %p.label Essays
    .train_line.essays
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
  .train_line_wrap
    %p.label Friends
    .train_line.friends
      - Album.all.each do |album|
        = link_to "", album, :class => "stop", :label => album.name
      - Album.limit(4).each do |album|
        = link_to "", album, :class => "stop", :label => album.name